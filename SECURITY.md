# Repository Policy

This repository is part of MADSuite.

The desktop agent must remain voluntary, transparent, and controllable by the user.

Do not commit real `.env` files, private values, customer data, production logs, or packaged binaries.

Default exclusions include camera, microphone, permanent screen capture, raw keylogging, biometrics, medical inference, external profiling, user comparison, and normality scoring.

Source of truth:

```text
bleeband/SYSTEME_MAD
```
